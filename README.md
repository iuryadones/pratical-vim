# Pratical vim and neovim

# Commands Vim

## Modo Normal

* cmd: `dib` or `di(` ou `di)`
 - (word1 word2) -> ()

* cmd: `di[` or `di]`
 - [word1 word2] -> []

* cmd: `diB` or `di{` or `di}`
 - {word1 word2} -> {}
